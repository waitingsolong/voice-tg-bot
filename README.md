1. Create an .env in the root like:
TELEGRAM_TOKEN = "zxc"
OPENAI_API_KEY = "99"
...

And specify all the variables listed in the src/config.py in the Settings class

2. Launch src/main.py 

3. Relax
